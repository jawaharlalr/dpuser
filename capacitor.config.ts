import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dpeveningsnacksandsweets.app',
  appName: 'DP Evening Snacks & Sweets',
  webDir: 'build'
};

export default config;
